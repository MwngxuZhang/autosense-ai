services:
  - type: web
    name: autosense-ai
    env: python
    plan: free
    buildCommand: pip install -r requirements.txt
    startCommand: python outputs/autosense_ai_full/server.py
    envVars:
      - key: PYTHON_VERSION
        value: 3.11.9
      - key: PORT
        value: 10000
